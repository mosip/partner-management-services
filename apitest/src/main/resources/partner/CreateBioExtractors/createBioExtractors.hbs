{ 
	"partnerId": "{{partnerId}}",
	"policyId": "{{policyId}}",
	  "id": "string",
	  "version": "string",
	  "requesttime": "{{requesttime}}",
	  "metadata": {},
	  "request": {
	    "extractors": [
	      {
	        "attributeName": "{{attributeName}}",
	        "biometric": "{{biometric}}",
	        "extractor": {
	          "provider": "{{provider}}",
	          "version": "{{version}}"
	        }
	      }
	    ]
	  }
}