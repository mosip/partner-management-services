#####  DB queries to be executed to tear down the data used and generated during the test rig run for partnerRevamp APIs

delete from mosip_keymgr.keymgr.ca_cert_store where cert_subject LIKE '%O=${runContext}%';
delete from mosip_keymgr.keymgr.partner_cert_store where cert_subject LIKE '%O=${runContext}%';
delete from mosip_keymgr.keymgr.key_alias where app_id = 'PARTNER' AND ref_id LIKE '%${runContext}%';