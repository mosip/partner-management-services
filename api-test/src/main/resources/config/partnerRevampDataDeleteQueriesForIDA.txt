#####  DB queries to be executed to tear down the data used and generated during the test rig run for partnerRevamp APIs

delete from ida.ca_cert_store where cert_subject like '%O=${runContext}%';
delete FROM ida.oidc_client_data where partner_id='${runContext}_pms-111999';