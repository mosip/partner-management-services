GetAllApprovedPartnerId:
   Pms_GetAllApprovedAuthPartnerId_without_auth:
      endPoint: /v1/partnermanager/partners/v3?status=approved&policyGroupAvailable=true&partnerType=Auth_Partner
      description: Validate that retrieving approved authenticated partner IDs fails when authentication is not provided
      uniqueIdentifier: TC_PMS_GetAllApprovedAuthPartnerId_01_neg
      role: invalidtoken
      restMethod: get
      inputTemplate: pms/GetAllApprovedPartnerId/getAllApprovedPartnerId
      outputTemplate: pms/error
      input: '{
}'
      output: '{
      "errors": [
      {
       "errorCode": "KER-ATH-401"
       }
       ]
}'
   Pms_GetAllApprovedAuthPartnerId_invalid_auth:
      endPoint: /v1/partnermanager/partners/v3?status=approved&policyGroupAvailable=true&partnerType=Auth_Partner
      description: Validate that retrieving approved authenticated partner IDs fails when an invalid authentication is provided
      uniqueIdentifier: TC_PMS_GetAllApprovedAuthPartnerId_02_neg
      role: invalidtoken
      restMethod: get
      inputTemplate: pms/GetAllApprovedPartnerId/getAllApprovedPartnerId
      outputTemplate: pms/error
      input: '{
}'
      output: '{
      "errors": [
      {
       "errorCode": "KER-ATH-401"
       }
       ]
}'