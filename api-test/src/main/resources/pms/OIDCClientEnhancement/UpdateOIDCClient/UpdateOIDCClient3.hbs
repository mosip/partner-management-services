{
	"id": "{{id}}",
	"version": "{{version}}",
	"requestTime": "{{requestTime}}",
	"clientId": "{{clientId}}",
	"request": {
		"logoUri": "{{logoUri}}",
		"redirectUris": [
			"{{redirectUris}}"
		],
		"status": "{{status}}",
		"grantTypes": [
			"{{grantTypes}}"
		],
		"clientName": "{{clientName}}",
		"clientAuthMethods": [
			"{{clientAuthMethods}}"
		],
		"clientNameLangMap": {
      "eng": "{{clientNameLangMap}}"
          }
	}
}
