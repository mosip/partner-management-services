{
    "response": {
        "pageNo": "{{pageNo}}",
        "partners": [
            {{#each partners}}
            {
                "logoUrl": "{{logoUrl}}",
                "additionalInfo": "{{additionalInfo}}"
            }
            {{#unless @last}},{{/unless}}
            {{/each}}
        ]
    }
}