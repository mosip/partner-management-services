{
  "response": {
    "data": [
     {{#each data}}
      {
        "policyType": "{{policyType}}",
        "schema": "{{schema}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}