{
  "id": "string",
  "version": "string",
  "requesttime": "{{requesttime}}",
  "metadata": {},
  "request": {
    "name": "{{name}}",
    "policyId": "{{policyId}}",
    "publicKey": {{publicKey}},
    "authPartnerId": "{{authPartnerId}}",
    "logoUri": "{{logoUri}}",
    "redirectUris": [
          "{{redirectUris}}"
  ],
    "grantTypes": [
            "{{grantTypes}}"
        ],
    "clientAuthMethods": [
            "{{clientAuthMethods}}"
        ],
    "clientNameLangMap": {
      "eng": "OIDC TEST CLIENT"
    }
  }
}