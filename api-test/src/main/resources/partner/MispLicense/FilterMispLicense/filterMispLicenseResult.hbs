{
  "response": {
    "filters": [
     {{#each filters}}
      {
        "fieldID": "{{fieldID}}",
        "fieldValue": "{{fieldValue}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}