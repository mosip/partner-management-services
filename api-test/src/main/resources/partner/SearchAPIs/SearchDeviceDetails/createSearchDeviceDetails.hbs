{
  "id": "string",
  "metadata": {},
  "request": {
    "deviceProviderId": "{{deviceProviderId}}",
    "filters": [
    {{#each filters}}
      {
        "columnName": "{{columnName}}",
        "type": "{{type}}",
        "value": "{{value}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ],
    "pagination": {
      "pageFetch": "{{pageFetch}}",
      "pageStart": "{{pageStart}}"
    },
	"purpose": "REGISTRATION",
    "sort": [
    	{{#each sort}}
      {
        "sortField": "{{sortField}}",
        "sortType": "{{sortType}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
},
  "requesttime": "{{requesttime}}",
  "version": "string"
}